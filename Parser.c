#include "Parser.h"

int parsekind(int toks[], int toks_size, int dag[], int dag_size);
int parsetype(int toks[], int toks_size, int dag[], int dag_size);
int parseterm(int toks[], int toks_size, int dag[], int dag_size);

int parsekind(int toks[], int toks_size, int dag[], int dag_size)
{
	return 0;
}

int parsetype(int toks[], int toks_size, int dag[], int dag_size)
{
	return 0;
}


int parseterm(int toks[], int toks_size, int dag[], int dag_size)
{
	return 0;
}
